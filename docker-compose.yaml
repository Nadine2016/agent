# AUTOGENERATED FILE: DO NOT EDIT
# Generated by the AgentRegistry CLI.

services:
  myagent:
    image: localhost:5001/myagent:latest
    build:
      context: .
      dockerfile: Dockerfile
    command: ["myagent", "--local"]
    ports:
      - "8080:8080"
    environment:
      # TODO: remove kagent variables (currently required)
      - KAGENT_URL=placeholder
      - KAGENT_NAME=placeholder
      - KAGENT_NAMESPACE=placeholder
      - AGENT_NAME=myagent
      - OTEL_SERVICE_NAME=myagent
      - OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=${OTEL_EXPORTER_OTLP_TRACES_ENDPOINT}
      - MODEL_PROVIDER=gemini
      - MODEL_NAME=gemini-2.0-flash
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
    volumes:
      - type: bind
        source: ./myagent
        target: /config

